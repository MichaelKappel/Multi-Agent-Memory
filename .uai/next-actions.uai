Next actions

Purpose:
- Ordered current next actions for MemoryEndpoints.com without becoming a dated task log.

Verification status: active operational memory; verify with `.uai/progress.uai`, exact-SHA live evidence, and current human instructions before relying on it.
Memory scope: next work, acceptance conditions, blockers, and verification targets.
Public-safe status: safe for source control; contains no credential values.
Update route: replace this file's current list when priority or blocker state changes; keep detailed history in protected MATM and point-in-time evidence under ignored `var/reports/`.
Source of truth: `.uai/progress.uai`, `.uai/open-questions.uai`, `.uai/test-plan.uai`, current source, and exact-SHA live evidence.
Next action for agents: perform the highest unblocked action that matches the current user request.
Must not expose: raw API keys, bearer tokens, FTP credentials, database passwords, private keys, private handoff payloads, raw one-time keys, or private deployment values.

Current ordered actions:
- Preserve the exact clean pushed revisions recorded in root `workspace.uai`. This public coordinator's pre-update parent is `7a10c84e0a6d5e8d62ebeb34dc18b2a0070687c5`; resolve the containing coordinator commit from Git after check-in rather than embedding a self-reference.
- MemoryEndpoints has the only current exact ready site package: private commit `97e0e5192e9f5ba9ae1c07ab209d39715b4e3586`, 293 files, 10,506,324 bytes, SHA-256 `2ca7fe6c15f09f78554737c0d43505fc565ea4f0f2f5757f2c7baaea8f2214d3`, and 44-route local exact-SHA WSGI verification. Resolve and bind the missing protected remote directory, migration preflight, and release authority before deploying the unchanged package; the current connection result is `missing_remote_dir`, `safeNoOp=true`, and zero mutation.
- LocalEndpoints is clean/pushed at `8c02d854eab30c929fc08f1fd0ac8339875333de`, but its immutable archive embeds source `1.5.261` and public/deployed `1.5.258`. Preserve it without upload or reinterpretation. Create a new versioned successor with aligned identities and independently executable truthful forward/retained verifier contracts.
- RemoteEndpoints is SOURCE/CHECKIN/PUSH GO at `26e7fd66f856738b22642eb32c09c0d5732846b2`. Establish the supported target runtime, trusted bootstrap/controller privilege boundary, protected atomic root/baseline, immutable package, and zero-overlap reload/rollback proof before any deployment attempt.
- Preserve checked-in Connect commit `317684dc34bd98bda5e09087e1063e094b439373`. Correct and independently qualify the ValidationOnly pytest-timeout contract, then run one fresh no-retry ValidationOnly lane to a success receipt before whole SOURCE GO. PACKAGE remains NO-GO until shipped binaries and every external identity, plugin, signing, and clean-install authority pass.
- Publish MultiAgentMemory.com only after a target-bound `multiagentmemory` profile, fresh immutable package from the final coordinator commit, activation authority, rollback, and live exact-revision proof exist. Historical FTPS success authorizes nothing.
- Build, sign, install, upload, activate, or deploy only a candidate that has the independently required PACKAGE, target, credential, rollback, and live-verification authorities. Otherwise perform a safe no-op and record the blocker.
- After the release-critical cross-product work is closed or explicitly blocked, select one source from the local report intake queue through canonical source-provenance preflight. Process it alone through scoped wiki pages, first-class citations, individually reviewed memories, exact readback, and learned-system improvements.
- For each report, preserve one canonical source, split distinct knowledge into focused pages, choose the narrowest durable scope, add multiple useful taxonomy paths, and classify non-current claims before they enter durable memory.
- Review every external citation independently as a first-class web record. Distinguish primary authority, vendor material, community discussion, preprints, blocked or stale paths, citation-to-claim mismatches, and jurisdiction errors.
- Keep relational SQLite and production MySQL behavior covered by integration, migration, route, and read-after-write tests.
- Do not keep trying GitHub Actions unless the human re-enables it; the human clarified it is not needed as a completion gate.
- Record SOURCE, PACKAGE, INSTALLED, DEPLOYMENT, and LIVE independently. Source completion never upgrades a later gate, and a blocked deployment must remain visibly blocked rather than triggering an upload.
- Repeat authenticated desktop and narrow-mobile human-wiki visual QA after every authorized deployed UI change; historical screenshots are behavior evidence, not current-revision proof.
